# Configuration

Future environment loading and startup validation. Reject missing credentials
for enabled adapters and keep mock, sandbox, and live modes explicit.
The root `.env.example` is a planning template, not loaded configuration.
