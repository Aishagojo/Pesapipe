# HTTP Entry Points

Future Express application, USSD handlers, provider webhooks, and health endpoint.
Keep request parsing, authentication, and response formatting here; call services
for transfer rules. No HTTP server is implemented yet.
